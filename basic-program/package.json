{
	"name": "basic-program",
	"module": "main.ts",
	"type": "module",
	"scripts": {
		"watch": "bun src/main.ts --watch",
		"build": "bunx tsc",
		"serve": "bun target/main.js",
		"test:all": "bun run test:bun && bun run test:another",
		"test:bun": "bun test",
		"test:another": "echo 'ok'; exit 0"
	},
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	}
}
